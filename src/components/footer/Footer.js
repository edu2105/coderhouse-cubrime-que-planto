import React from "react";
import '../../stylesheets/Footer.css'

const Footer = (props) => {
    return(
        <footer className="footer">
            <h1>En construcción, vuelva pronto.</h1>
        </footer>
    );
};

export default Footer;